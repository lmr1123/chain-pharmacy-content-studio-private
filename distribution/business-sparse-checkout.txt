# Business install sparse-checkout (non-cone patterns for Git sparse-checkout set).
# One pattern per line. Comments and blank lines ignored.
#
# Goal: smaller business clone WITHOUT dropping production quality assets:
#   KEEP settled gold PPTX/MP4, engines, assets/component-library, video kit
#   DROP research trees (poc/reference-replica, validation evidence, etc.)
#
# Root files (README, AGENTS.md, …) stay via /* with !/*/ then re-add dirs.

/*
!/*/
/scripts/
/docs/
/distribution/
/production-library/
!/production-library/validation/
/assets/component-library/
/poc/gold-sample/
/outputs/业务使用资料包/
/public-entry/
/skills/
