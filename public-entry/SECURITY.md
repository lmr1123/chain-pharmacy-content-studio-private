# 安全说明

本仓库只发布脱敏安装入口，不发布模板、声音、金样、业务文档或生产资产。

安装器仅访问 GitHub 官方主机，并同时接受 GitHub CLI 正常生成的 HTTPS 或 SSH 官方 `origin`。任何代理、镜像或其他远端都会阻断安装。

不要在 Issue、日志、聊天或命令参数中提交访问令牌、业务资料或授权资产。发现 Public 树包含秘密或内部资产时，应停止发布并通过组织内部安全渠道报告。
